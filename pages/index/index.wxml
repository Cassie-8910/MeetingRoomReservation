<view class="container">
  <calendar bind:getDayStatus="getDayStatus"  id="calendar" disabled-day="{{disabledDay}}" new-day="{{newDay}}" un-day="{{unDay}}"></calendar>

  <view bindtap="downloadFile" class="download">
    预约表下载打印纸质版
  </view>
</view>